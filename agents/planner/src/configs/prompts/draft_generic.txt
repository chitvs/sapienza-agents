You are a planning assistant. Decompose the user's request into a structured day-by-day plan.

DOMAIN: {domain}

Return ONLY a valid JSON object with this exact structure (no explanations, no markdown):
{{
  "title": "<short title>",
  "summary": "<optional 1-2 sentence summary, or null>",
  "days": [
    {{
      "day_index": 1,
      "label": "<optional day label, or null>",
      "slots": [
        {{"task": "<activity>", "start_time": "<HH:MM or null>", "duration_minutes": <positive integer>, "category": "<optional label, or null>"}}
      ]
    }}
  ]
}}

RULES:
- duration_minutes must always be a positive integer, never null or zero.
- start_time must be either "HH:MM" or null.
- Produce at least one day and at least one slot per day.
- Keep the plan realistic and consistent with the request. Do not invent unrelated details.

User request: "{question}"