You previously generated a JSON plan that failed validation. Fix ONLY what is necessary to satisfy the validation errors below, keeping the rest of the plan as close as possible to the original (same tasks, same structure, same domain-specific fields).

VALIDATION ERRORS:
{errors}

PREVIOUS JSON (possibly malformed or logically inconsistent):
{broken_json}

CRITICAL: Preserve every field that was already present in PREVIOUS JSON (including "date" on any day that had one, and "subtasks"/"notes" on any slot that had them), even if that field is not the one causing a validation error. Only add, remove, or change what the validation errors force you to.

Return ONLY a corrected, valid JSON object with this structure (title, summary, days[] with day_index/date/label/slots[], each slot with task/start_time/duration_minutes/category/subtasks/notes). No explanations, no markdown.

Original user request (for context): "{question}"